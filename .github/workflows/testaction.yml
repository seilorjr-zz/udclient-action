name : test-action 

on:
  workflow_dispatch:
  
jobs:
  build:
    name: test udclient cli  
    runs-on: ubuntu-latest   
    steps:
     - name: call udclient
       uses: seilorjr/udclient-action@main
       with:
        command: |
          java -jar udclient.jar --weburl ${{env.WEBURL}} --username ${{env.UDUSER}} --password ${{env.UDPASSWROD}}  createVersion --component lala --name 1.0
